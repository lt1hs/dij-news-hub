import NewsFeed from "../NewsFeed";

export default function NewsFeedExample() {
  return (
    <NewsFeed
      deskMode="compact"
      setDeskMode={() => {}}
      commandOpen={false}
      setCommandOpen={() => {}}
    />
  );
}
